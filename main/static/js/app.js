

$(function() {
    $('.card').matchHeight();


$(function() {
    $('.equal-height').matchHeight();

$('.mylightbox').featherlight($content, configuration);


                $(function() {
                    $.scrollify({
                        section : ".sexy-cards",
                    });
                });

    $(document).foundation();

    $(function() {
       $('#demo').scrollbox({
         linear: true,
         step: 1,
         delay: 0,
         speed: 3000
       });
     });
